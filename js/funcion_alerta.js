function funcionAlerta() {
  alert("Solicitud enviada con éxito");
  window.location = "principal.php";
}
/*function enviar() {
  //event.preventDefault();
  alert("La solicitud se envió correctamente.- <br> Gracias");
  window.location = "index.php";
}

function validacion() {
  //event.preventDefault();
  //alert("La solicitud se envió correctamente.");
  //window.location = "index.php";
}*/

/*document.addEventListener("DOMContentLoaded", function () {
  document
    .getElementById("formulario")
    .addEventListener("submit", validarFormulario);
});

function validarFormulario(evento) {
  evento.preventDefault();

  var nombre_apellido = document.getElementById("nombre_apellido").value;
  if (nombre_apellido.length == 0) {
    alert("Debe ingresar el Nombre y Apellido");
    return;
  }
  /*var clave = document.getElementById("clave").value;
  if (clave.length < 6) {
    alert("La clave no es válida");
    return;
  }*/

/*var area = document.getElementById("area").value;
  if (area.length == 0) {
    alert("Debe ingresar el Área");
    return;
  }
  var motivo_solicitud = document.getElementById("motivo_solicitud").value;
  if (motivo_solicitud.length == 0) {
    alert("Debe ingresar el motivo de la solicitud");
    return;
  }
  var tipo_solicitud = document.getElementById("tipo_solicitud").value;
  if (tipo_solicitud.length == 0) {
    alert("Debe ingresar el tipo de solicitud");
    return;
  }
  var prioridad = document.getElementById("prioridad").value;
  if (prioridad.length == 0) {
    alert("Debe ingresar la prioridad");
    return;
  }
  window.location = "enviar_solicitud.php";
  this.submit();
}
*/
